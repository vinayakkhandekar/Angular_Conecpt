import { NormalDirective } from './normal.directive';

describe('NormalDirective', () => {
  it('should create an instance', () => {
    const directive = new NormalDirective();
    expect(directive).toBeTruthy();
  });
});
